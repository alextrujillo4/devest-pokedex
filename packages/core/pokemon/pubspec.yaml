name: pokemon
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.3.2 <4.0.0'
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter

  dartz: ^0.10.1
  http: ^1.0.0
  get_it: ^7.1.3
  intl: ^0.19.0
  cached_network_image: ^3.3.1

  network:
    path: ../../common/network
  storage:
    path: ../../common/storage
  failure:
    path: ../../common/failure
  state_manager:
    path: ../../common/state_manager



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  lints: ^3.0.0
  mockito: ^5.4.2
  build_runner: ^2.4.8
